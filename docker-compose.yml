version: '3.8'

services:
  neuro-cosmos:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    container_name: neuro-cosmos-web
    ports:
      - "5173:80"
    restart: unless-stopped
